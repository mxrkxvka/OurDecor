<Window x:Class="OurDecor.AddEditProductWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Добавить / Редактировать продукт"
        Height="650" Width="700"
        WindowStartupLocation="CenterOwner"
        Icon="Resources/logo.ico"
        FontFamily="{StaticResource MainFont}"
        FontSize="20"
        Background="{StaticResource BackgroundBrush}">

    <!-- ScrollViewer для всей формы -->
    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
        <Grid Margin="15">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Заголовок -->
            <TextBlock Grid.Row="0" Text="Добавить / Редактировать продукт"
                       FontSize="26"
                       FontWeight="Bold"
                       Foreground="{StaticResource PrimaryBrush}"
                       HorizontalAlignment="Center"
                       Margin="0,0,0,15"/>

            <!-- Форма продукта -->
            <Grid Grid.Row="1" Margin="0,0,0,10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="180"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Text="Артикул:" Grid.Row="0" Grid.Column="0" VerticalAlignment="Center" Margin="0,5"/>
                <TextBox x:Name="ArticleTextBox" Grid.Row="0" Grid.Column="1" Height="40" Margin="0,5" Padding="6"/>

                <TextBlock Text="Тип продукта:" Grid.Row="1" Grid.Column="0" VerticalAlignment="Center" Margin="0,5"/>
                <ComboBox x:Name="ProductTypeComboBox" Grid.Row="1" Grid.Column="1"
                          Height="36" Margin="0,5"
                          DisplayMemberPath="Name"
                          SelectedValuePath="ProductTypeID"/>

                <TextBlock Text="Наименование:" Grid.Row="2" Grid.Column="0" VerticalAlignment="Center" Margin="0,5"/>
                <TextBox x:Name="NameTextBox" Grid.Row="2" Grid.Column="1" Height="40" Margin="0,5" Padding="6"/>

                <TextBlock Text="Мин. стоимость (р):" Grid.Row="3" Grid.Column="0" VerticalAlignment="Center" Margin="0,5"/>
                <TextBox x:Name="MinCostTextBox" Grid.Row="3" Grid.Column="1" Height="40" Margin="0,5" Padding="6"/>

                <TextBlock Text="Ширина (м):" Grid.Row="4" Grid.Column="0" VerticalAlignment="Center" Margin="0,5"/>
                <TextBox x:Name="WidthTextBox" Grid.Row="4" Grid.Column="1" Height="40" Margin="0,5" Padding="6"/>
            </Grid>

            <!-- Материалы -->
            <GroupBox Grid.Row="2" Header="Материалы" FontWeight="Bold" Margin="0,0,0,10">
                <DockPanel Margin="10" Cursor="">
                    <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="0,0,0,10">
                        <ComboBox x:Name="MaterialComboBox"
                                  Width="300"
                                  DisplayMemberPath="Name"
                                  SelectedValuePath="MaterialID"
                                  Height="40"
                                  Margin="0,0,8,0"/>
                        <TextBox x:Name="MaterialCountTextBox" Width="80" Margin="0,0,8,0" Height="40"
                                 Foreground="Gray" Text="Кол-во"
                                 GotFocus="MaterialCountTextBox_GotFocus"
                                 LostFocus="MaterialCountTextBox_LostFocus"/>
                        <Button Content="Добавить" Click="AddMaterialButton_Click" Width="100" Style="{StaticResource PrimaryButton}" FontSize="20"/>
                    </StackPanel>

                    <DataGrid x:Name="MaterialsDataGrid"
          AutoGenerateColumns="False"
          CanUserAddRows="False"
          IsReadOnly="True"
          Height="200"
          RowBackground="White"
          AlternatingRowBackground="#F5F5F5"
          GridLinesVisibility="Horizontal"
          HorizontalAlignment="Stretch">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Материал" Binding="{Binding MaterialName}" Width="*"/>
                            <DataGridTextColumn Header="Количество" Binding="{Binding Count}" Width="100"/>
                            <DataGridTextColumn Header="Цена (р.)" Binding="{Binding Price}" Width="100"/>
                            <DataGridTextColumn Header="Тип" Binding="{Binding MaterialType}" Width="150"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </DockPanel>
            </GroupBox>

            <!-- Кнопки -->
            <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
                <Button x:Name="CancelButton" Content="Отмена" Width="120" Height="45" Margin="0,0,10,0" Click="BackButton_Click"/>
                <Button x:Name="SaveButton" Content="Сохранить" Width="160" Height="45" Click="SaveButton_Click" Style="{StaticResource PrimaryButton}"/>
            </StackPanel>
        </Grid>
    </ScrollViewer>
</Window>
